@{ Layout = "~/Views/Shared/_Layout.cshtml"; }

@model IEnumerable<GYM.Models.ApplicationUser>

<br />


<br />
<div class="container mt-5">
    <div class="card">

        <div class="card-body">
            <div class="row">
                @*<div class="col-6">
                <h2 class="text-primary">User List</h2>
            </div>*@
                <div class="col-12">
                    @*<a class="btn btn-primary" asp-area="Identity" asp-page="/Account/Register"><i class="fas fa-plus"></i> &nbsp; Create New User</a>*@
                    <a class="btn btn-primary" asp-action="Upsert"><i class="fas fa-plus"></i> &nbsp; Crear nuevo usuario</a>
                </div>
                <br />
            </div>
            <div class="p-4 border rounded">
                <table id="tblData" class="table table-striped table-bordered" style="width:100%">
                    <thead class="thead-dark">
                        <tr class="text-white">
                            <th>Name</th>
                            <th>Email</th>
                            <th>Role</th>
                            <th>Options</th>
                            <th>Lock/Unlock</th>
                        </tr>
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>

            @section Scripts{
                <script src="~/js/User.js"></script>
            }


